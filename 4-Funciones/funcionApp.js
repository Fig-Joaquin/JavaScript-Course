// Declaración de Función (Function Definition)

function suma(a, b) {
    return a + b;
}

console.log(suma(2, 3)); 


// Expresión de función (Function Expression)
const resta = function(a, b) {
    return (a - b);
};

console.log(resta(5, 3));